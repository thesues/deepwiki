# ⚠️ IMAGE_AUTUMN_RS needs the autumn-rs image tag the cluster is running.
#
# memory-mcp over the documents stored in autumn.
#
# The two namespaces play different roles here and it is worth being explicit:
# the DOCUMENTS live in `fs/` and are read through a FUSE mount, because
# memory-mcp ingests from a filesystem path (`--docs`, and the MCP
# `ingest_documents` tool takes a server-side path, not content). The INDEX it
# builds — BM25 postings, IVF vectors, the CONTAINS outline graph — is written
# to `mem/`. So this pod reads fs/ and writes mem/, and needs a credential for
# each.
#
# Corpus: `fs/docs/buda/` (17 Chinese Buddhist texts, ~2.1M chars, 5164 chunks
# when ingested locally). `--no-index` skips the Rust source indexer — there is
# no code tree in this pod and the docs corpus is the whole point.
---
apiVersion: v1
kind: Service
metadata:
  name: memory-mcp
  namespace: autumn
  labels: { app.kubernetes.io/name: autumn, app.kubernetes.io/component: memory-mcp }
spec:
  # ClusterIP: the HTTP surface is read-only but unauthenticated, same posture
  # as the dashboard. Reach it with `kubectl port-forward`.
  type: ClusterIP
  selector: { app.kubernetes.io/name: autumn, app.kubernetes.io/component: memory-mcp }
  ports:
    - { name: http, port: 5100, targetPort: 5100 }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory-mcp
  namespace: autumn
  labels: { app.kubernetes.io/name: autumn, app.kubernetes.io/component: memory-mcp }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: autumn, app.kubernetes.io/component: memory-mcp }
  template:
    metadata:
      labels: { app.kubernetes.io/name: autumn, app.kubernetes.io/component: memory-mcp }
    spec:
      enableServiceLinks: false
      nodeSelector: { autumn-node: "true" }

      # ── One container, not a fuse sidecar ────────────────────────────────
      # The sidecar shape does not work on this cluster. `mountPropagation:
      # Bidirectional` is accepted by the API server but the node never honours
      # it: measured inside a privileged sidecar, the container had ZERO shared
      # mounts and the FUSE mount came up `PROPAGATION: private` with the
      # container root as its parent. Nothing propagates, so the app container
      # saw an empty directory and died on "No such file or directory".
      #
      # Running the mount in the SAME container sidesteps propagation entirely —
      # one mount namespace, no sharing required. This is the shape the earlier
      # O_DIRECT probe used, and it worked.
      #
      # The cost is honest: the whole container is privileged now, where before
      # only the sidecar was. Reverting to a sidecar requires the node's kubelet
      # to be configured with rshared propagation, which is a cluster-level
      # change on shared infrastructure.
      #
      # 2026-09-02 — there is now a SECOND and stronger reason not to go back.
      # Bidirectional propagation is precisely what lets a FUSE mount escape into
      # the host mount namespace and outlive the pod. A mount left behind with no
      # server blocks every stat() that crosses it, in uninterruptible sleep, and
      # a container runtime stats mount points on every sandbox create and
      # teardown — so one leaked mount stops the NODE from starting containers
      # while everything already running carries on normally. We lost five nodes
      # to that shape in one afternoon and only a reboot cleared them. The
      # autumn-rs fix (MountOption::AutoUnmount, plus a bounded async read so a
      # live-but-stalled daemon can't park the kernel either) landed in 344a37f;
      # do not run any build older than that here, and if a sidecar ever does
      # become possible, weigh this against it, not just the propagation
      # mechanics.
      containers:
        - name: memory-mcp
          image: IMAGE_AUTUMN_RS
          securityContext: { privileged: true }
          command: ["bash", "-lc"]
          args:
            - |
              set -euo pipefail
              # IP-only client: resolve the manager before dialing it.
              M="$(getent hosts autumn-manager | awk '{print $1}'):9001"
              MP=/mnt/autumn
              mkdir -p "$MP"

              # A crashed predecessor can leave the mount entry behind; clear it
              # before remounting or fusermount fails with EBUSY.
              # Never `mountpoint -q` a possibly-stale FUSE path: it stats, and
              # a stale mount blocks stat() forever — so the check meant to
              # recover from a crashed predecessor is what hangs its
              # replacement. /proc/mounts is pure VFS metadata; the unmount is
              # lazy so it cannot block on the dead connection either.
              if grep -qs " $MP fuse" /proc/mounts; then
                fusermount3 -uz "$MP" 2>/dev/null || umount -l "$MP" 2>/dev/null || true
              fi

              # --direct-read false is required: reads >= 64 KiB go EN-direct and

              # cannot work on EC-converted extents (the .dat holds RS shards), and

              # the corpus files are up to 820 KB. Without it every document read

              # fails and ingest silently indexes ZERO chunks while the server comes

              # up healthy — measured: 17 files, 389 s, 0 chunks.

              autumn-fuse --manager "$M" --mountpoint "$MP" --transport tcp --direct-read false \
                --credential-file /etc/autumn/cred/fs.cred &
              FUSE_PID=$!
              trap 'kill $FUSE_PID 2>/dev/null; fusermount3 -u "$MP" 2>/dev/null' EXIT

              # Wait for a real READ, not for the mount entry. The kernel adds
              # the entry BEFORE it sends FUSE INIT, so a mountpoint/proc check
              # goes true while the handshake is still outstanding — and if INIT
              # then times out, everything after this point hits a dead mount.
              # That crash-looped the freetoken pod: "mounted" logged, init
              # failed 30 s later, first ls died with "Connection refused".
              # Wait until the mount serves the path we actually need.
              #
              # Two weaker checks both pass for the wrong reason. /proc/mounts and
              # `mountpoint -q` go true BEFORE the kernel sends FUSE INIT, so they
              # are satisfied while the handshake is still outstanding. And `ls
              # "$MP"` succeeds on the bare mountpoint before the mount takes over,
              # because listing an empty directory is a success — that raced too,
              # logging "mounted after 1s" before the daemon had even connected,
              # and then dying on the first real path. Only reading something that
              # cannot exist until the filesystem is live actually proves it.
              for i in $(seq 1 120); do
                ls "$MP/docs/buda" >/dev/null 2>&1 && break
                kill -0 $FUSE_PID 2>/dev/null || { echo "autumn-fuse exited during mount"; exit 1; }
                sleep 1
              done
              ls "$MP/docs/buda" >/dev/null 2>&1 || { echo "mount not serving "$MP/docs/buda" after 120s"; exit 1; }
              echo "mounted; corpus:"; ls "$MP/docs/buda" | head -5

              # The vector leg comes from llama-embed (BGE-M3 on a 4090) — see
              # k8s/llama-embed.yaml. Without --embed-url this serves BM25 only
              # and says so; it does not fall back to something that answers
              # vector queries with noise.
              #
              # --reset wipes mem/{tenant}/{agent}/ and rebuilds. It is right
              # exactly once — when what is stored was written by a version
              # that spelled it differently — and it is a mistake to leave on:
              # every restart would re-ingest the corpus and re-embed it, so a
              # pod that reschedules at 3am spends that time rebuilding an
              # index it already had.
              #
              # It was on for one roll, at autumn-rs 7190693, because the
              # ids changed there — and that was the last time they can. They
              # used to be `/mnt/autumn/docs/buda/x.md`: readable, and still
              # wrong, because `/mnt/autumn` is THIS pod's `--mountpoint` and
              # it had no business inside 7433 stored keys. An id is the
              # autumnfs path now (`docs/buda/x.md`), so nothing about where
              # a client mounts can invalidate the index again. The wipe cost
              # 2.9M keys and the re-ingest 168 s; that is what this flag
              # costs EVERY restart, which is why it is off.
              #
              # `--index` takes an autumnfs directory, never a host path, and
              # `--fs-root` is the one place the mountpoint appears. No
              # `--code`: this corpus is prose, and the tree-sitter pass only
              # speaks Rust.
              exec memory-mcp "$M" \
                --index docs/buda \
                --fs-root "$MP" \
                --tenant buda \
                --embed-url http://llama-embed:8080 \
                --embed-model bge-m3 \
                --credential-file /etc/autumn/cred/mem.cred \
                --host 0.0.0.0 --port 5100
          ports:
            - { name: http, containerPort: 5100 }
          volumeMounts:
            - { name: cred, mountPath: /etc/autumn/cred, readOnly: true }
          resources:
            requests: { cpu: "2", memory: 4Gi }
            limits:   { cpu: "4", memory: 8Gi }
          startupProbe:
            # Mount, then ingest; the HTTP listener only binds once ingest
            # finishes, so this budget has to cover the whole thing.
            #
            # Measured in-cluster: 5164 chunks in 168 s (30.7 chunk/s).
            #
            # It was ~45 min before the BM25 postings were batched — that version
            # paid one round trip per unique term, and CJK unigrams give a
            # Chinese chunk several hundred of them. Batching made the network
            # stop being the limit, so this is now bounded by real work.
            #
            # That 30.7 chunk/s was the in-process hash embedder. Sending every
            # chunk to a real model turned out to cost nothing: BGE-M3 on a 4090
            # measured 7433 chunks in 163 s — 45.7 chunk/s, FASTER than the fake
            # one, because the embedding was never the bottleneck.
            #
            # The same model on 6 CPU cores was ~6 chunk/s, which is ~20 min for
            # this corpus, and that is why the budget is what it is rather than
            # what today's number would justify: it has to survive the GPU being
            # busy or absent. The old 10 min did the worst possible thing when
            # ingest ran long — killed it minutes from the end, and the restart
            # began again from zero, forever.
            #
            # Too small remains the worse error by a wide margin. A budget that
            # is too large costs one slow failure; one that is too small costs
            # an infinite loop that looks like progress.
            # /config, NOT /stats. `/stats` runs a full `reconcile()` — a
            # scan of every doc, vptr, node and edge — which takes seconds on a
            # 5k-document corpus, while a probe's default timeout is 1s. The
            # pod then sits 0/1 forever with "context deadline exceeded" while
            # the server is up and serving, i.e. the check is more expensive
            # than the thing it checks. /config is static JSON.
            httpGet: { path: /config, port: 5100 }
            periodSeconds: 15
            failureThreshold: 180     # 180 x 15s = 45 min
          readinessProbe:
            # /config, NOT /stats. `/stats` runs a full `reconcile()` — a
            # scan of every doc, vptr, node and edge — which takes seconds on a
            # 5k-document corpus, while a probe's default timeout is 1s. The
            # pod then sits 0/1 forever with "context deadline exceeded" while
            # the server is up and serving, i.e. the check is more expensive
            # than the thing it checks. /config is static JSON.
            httpGet: { path: /config, port: 5100 }
            periodSeconds: 15

      volumes:
        - name: cred
          secret: { secretName: autumn-credential }
